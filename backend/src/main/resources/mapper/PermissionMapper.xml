<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.esfile.mapper.PermissionMapper">

    <!-- 结果映射 -->
    <resultMap id="PermissionResultMap" type="com.esfile.entity.mybatis.Permission">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="permission_name" property="permissionName" jdbcType="VARCHAR"/>
        <result column="permission_code" property="permissionCode" jdbcType="VARCHAR"/>
        <result column="permission_type" property="permissionType" jdbcType="INTEGER"/>
        <result column="parent_id" property="parentId" jdbcType="BIGINT"/>
        <result column="url" property="url" jdbcType="VARCHAR"/>
        <result column="icon" property="icon" jdbcType="VARCHAR"/>
        <result column="sort" property="sort" jdbcType="INTEGER"/>
        <result column="status" property="status" jdbcType="INTEGER"/>
        <result column="description" property="description" jdbcType="VARCHAR"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
        <result column="create_by" property="createBy" jdbcType="BIGINT"/>
        <result column="update_by" property="updateBy" jdbcType="BIGINT"/>
    </resultMap>

    <!-- 基础字段 -->
    <sql id="Base_Column_List">
        id, permission_name, permission_code, permission_type, parent_id, url, icon, 
        sort, status, description, create_time, update_time, create_by, update_by
    </sql>

    <!-- 查询所有权限 -->
    <select id="selectAll" resultMap="PermissionResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM permission
        ORDER BY sort ASC, create_time DESC
    </select>

    <!-- 根据ID查询权限 -->
    <select id="selectById" parameterType="java.lang.Long" resultMap="PermissionResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM permission
        WHERE id = #{id}
    </select>

    <!-- 根据权限编码查询权限 -->
    <select id="selectByPermissionCode" parameterType="java.lang.String" resultMap="PermissionResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM permission
        WHERE permission_code = #{permissionCode}
    </select>

    <!-- 根据父级权限ID查询权限列表 -->
    <select id="selectByParentId" parameterType="java.lang.Long" resultMap="PermissionResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM permission
        WHERE parent_id = #{parentId}
        ORDER BY sort ASC, create_time DESC
    </select>

    <!-- 根据权限类型查询权限列表 -->
    <select id="selectByType" parameterType="java.lang.Integer" resultMap="PermissionResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM permission
        WHERE permission_type = #{permissionType}
        ORDER BY sort ASC, create_time DESC
    </select>

    <!-- 根据角色ID查询权限列表 -->
    <select id="selectByRoleId" parameterType="java.lang.Long" resultMap="PermissionResultMap">
        SELECT p.<include refid="Base_Column_List"/>
        FROM permission p
        JOIN role_permission rp ON p.id = rp.permission_id
        WHERE rp.role_id = #{roleId}
        ORDER BY p.sort ASC, p.create_time DESC
    </select>

    <!-- 根据用户ID查询权限编码列表 -->
    <select id="selectPermissionCodesByUserId" parameterType="java.lang.Long" resultType="java.lang.String">
        SELECT DISTINCT p.permission_code
        FROM permission p
        JOIN role_permission rp ON p.id = rp.permission_id
        JOIN user_role ur ON rp.role_id = ur.role_id
        WHERE ur.user_id = #{userId}
        AND p.status = 1
    </select>

    <!-- 插入权限 -->
    <insert id="insert" parameterType="com.esfile.entity.mybatis.Permission" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO permission (
            permission_name, permission_code, permission_type, parent_id, url, icon, 
            sort, status, description, create_time, update_time, create_by, update_by
        ) VALUES (
            #{permissionName}, #{permissionCode}, #{permissionType}, #{parentId}, #{url}, #{icon}, 
            #{sort}, #{status}, #{description}, #{createTime}, #{updateTime}, #{createBy}, #{updateBy}
        )
    </insert>

    <!-- 更新权限 -->
    <update id="updateById" parameterType="com.esfile.entity.mybatis.Permission">
        UPDATE permission
        <set>
            <if test="permissionName != null">permission_name = #{permissionName},</if>
            <if test="permissionCode != null">permission_code = #{permissionCode},</if>
            <if test="permissionType != null">permission_type = #{permissionType},</if>
            <if test="parentId != null">parent_id = #{parentId},</if>
            <if test="url != null">url = #{url},</if>
            <if test="icon != null">icon = #{icon},</if>
            <if test="sort != null">sort = #{sort},</if>
            <if test="status != null">status = #{status},</if>
            <if test="description != null">description = #{description},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
        </set>
        WHERE id = #{id}
    </update>

    <!-- 根据ID删除权限 -->
    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE FROM permission WHERE id = #{id}
    </delete>

    <!-- 批量删除权限 -->
    <delete id="batchDeleteByIds">
        DELETE FROM permission WHERE id IN
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <!-- 删除角色权限关联 -->
    <delete id="deleteRolePermissions" parameterType="java.lang.Long">
        DELETE FROM role_permission WHERE permission_id = #{permissionId}
    </delete>

</mapper>
